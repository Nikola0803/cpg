'use strict';

/**
 * care-provider-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::care-provider-subscription.care-provider-subscription');
