'use strict';

/**
 * care-provider-subscription controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::care-provider-subscription.care-provider-subscription');
