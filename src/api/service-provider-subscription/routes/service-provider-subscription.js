'use strict';

/**
 * service-provider-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-provider-subscription.service-provider-subscription');
